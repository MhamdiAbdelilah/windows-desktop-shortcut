# Desktop switcher

the Desktop switcher is just simple AutoHotKey progame that allow the user to switch bettwen the desktops using super+the number of desktop.

> **Note** :
> super is the windows / start key
